Penganalan_Pola_Motor metode K-Nearest Neighbor fitur integral proyeksi dari tepi canny

menggunakan octave dengan tambahan package io & image

pkg install -forge (package)
pkg load (package)

lalu jalankan gui.m
